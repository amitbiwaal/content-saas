/**
 * Application shell.
 *
 * Scaffolded in Sprint 0 despite having no backend, because the API is frozen
 * and the UI track is the largest parallelization opportunity
 * (`17-implementation/repository-structure.md`).
 *
 * Screens, routing, and the design system arrive in Sprint 5
 * (`15-application-ui/`). Nothing here anticipates them.
 */
export default function Page() {
  return (
    <main>
      <h1>ContentOS</h1>
      <p>Application shell. Screens arrive in Sprint 5.</p>
    </main>
  );
}
