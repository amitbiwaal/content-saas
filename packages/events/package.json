{
  "name": "@contentos/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Transactional outbox, relay, dispatcher, registry, retry, DLQ, idempotency, replay (ADR-020/027/028).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@contentos/contracts": "workspace:*"
  },
  "devDependencies": {}
}
